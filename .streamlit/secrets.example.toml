AWS_ACCESS_KEY_ID = "replace-me"
AWS_SECRET_ACCESS_KEY = "replace-me"
AWS_DEFAULT_REGION = "us-east-1"

S3_BUCKET = "replace-me"
S3_DB_KEY = "accounting/ledger.db"
S3_BACKUP_PREFIX = "accounting/backups/"

EMAIL_SENDER = "account@domain.suffix"

# Optional Read-Only mode setting (set to true for read-only app deployment)
# READ_ONLY = false